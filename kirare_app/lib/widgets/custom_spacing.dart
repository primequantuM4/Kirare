import 'package:flutter/material.dart';

class CustomSpacing extends StatelessWidget {
  const CustomSpacing({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 35,
    );
  }
}
